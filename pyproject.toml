[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "habitalytics-config"
version = "1.0.0"
description = "Configuration package for Habitalytics project"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
# Packages are in root directory